# 1. ベースとなるDockerイメージを指定
# 軽量なWebサーバーであるNginxの公式イメージを利用します
FROM nginx:alpine

# 2. プロジェクトのファイルをコンテナ内にコピー
# カレントディレクトリ（.）の全ファイルを、Nginxがコンテンツを公開するディレクトリにコピーします
COPY . /usr/share/nginx/html

# 3. コンテナの80番ポートを公開することを指定
EXPOSE 80